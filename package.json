{
  "name": "find-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:api": "npm --prefix frontend run dev:api",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
